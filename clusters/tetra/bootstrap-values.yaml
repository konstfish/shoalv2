components:
  infra:
    management:
      argocd:
        enabled: true
      olm:
        enabled: true
      operators:
        enabled: true
        basePath: overlays/tetra
      ccm-hcloud:
        enabled: true
        basePath: overlays/tetra
    storage:
      local-path-provisioner:
        enabled: true
      hcloud-csi:
        enabled: true
        basePath: base
    ingress:
      ingress-nginx:
        enabled: false
      cert-manager:
        enabled: true
      istio-gateway:
        enabled: true
        basePath: overlays/tetra
    network:
      cilium:
        enabled: true
        basePath: overlays/tetra
      istio:
        enabled: true
        basePath: overlays/tetra
      netbird-operator:
        enabled: true
        basePath: overlays/tetra
    compute:
      knative:
        enabled: true
        basePath: base
    observability:
      prometheus:
        enabled: true
        basePath: overlays/tetra
      grafana:
        enabled: true
        basePath: overlays/tetra