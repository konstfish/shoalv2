apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: proxy-konst-fish
spec:
  secretName: proxy-konst-fish
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  commonName: "proxy.konst.fish"
  dnsNames:
  - "proxy.konst.fish"
  - "*.proxy.konst.fish"