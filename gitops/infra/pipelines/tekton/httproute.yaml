apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: tekton-dashboard
spec:
  parentRefs:
  - name: http-gateway-netbird
    namespace: istio-ingress
    sectionName: https
  hostnames:
  - "tekton.barracuda.net.konst.fish"
  rules:
  - backendRefs:
    - name: tekton-dashboard
      port: 9097